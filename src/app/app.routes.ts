import { Routes } from '@angular/router';
import { LoginFormComponent } from './login-screen/login-screen.component';

export const routes: Routes = [
  {
    path: "login",
    component: LoginFormComponent,
  }
];
